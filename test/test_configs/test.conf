# configuration for tests
[queue test-queue]
events = [(0, 10), (10, 5), (30, 10)]

[queue test-queue-2]
events = [(0, 1), (100, 100)]

[space test]
processors_limit = 10
api = test

[machinetype test test-type]
target_share = 1.0
flavor_names = null
# For tests we want this in terms of cycles?
backoff_seconds = 10
fizzle_seconds = 10
user_data = None
# test specific options
queue = test-queue
