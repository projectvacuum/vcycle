# configuration for tests
[queue test-queue]
events = [(0, 5000)]

[queue test-queue-2]
events = [lambda x: (x, 100) for x in range(100)]

[space test]
processors_limit = 1000
api = test

[machinetype test test-type]
target_share = 0.8
flavor_names = null
# For tests we want this in terms of cycles?
backoff_seconds = 10
fizzle_seconds = 10
user_data = None
# test specific options
queue = test-queue

[machinetype test test-type-2]
target_share = 0.2
flavor_names = null
# For tests we want this in terms of cycles?
backoff_seconds = 10
fizzle_seconds = 10
user_data = None
# test specific options
queue = test-queue-2
