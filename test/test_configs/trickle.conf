# one queue
[queue test-queue]
events = [(0, 100000)]

[queue test-queue-2]
events = [(i, 5) for i in range(1000)]

[space test]
processors_limit = 1000
api = test

[machinetype test low-priority-machine]
preemptible = True
target_share = 1
flavor_names = null
# For tests we want this in terms of cycles?
backoff_seconds = 10
fizzle_seconds = 10
user_data = None
# test specific options
queue = test-queue

[machinetype test high-priority-machine]
target_share = 1
flavor_names = null
# For tests we want this in terms of cycles?
backoff_seconds = 10
fizzle_seconds = 10
user_data = None
# test specific options
queue = test-queue-2
